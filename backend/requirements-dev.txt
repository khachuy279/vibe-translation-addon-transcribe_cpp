# ============================================================================
# Vibe Translation Addon — Backend DEV/TEST dependencies
#
# Cài đặt (kèm ràng buộc CUDA để pip không hạ torch — xem README mục
# "Nâng cấp gói phụ thuộc mà không phá torch CUDA"):
#     pip install -r backend/requirements-dev.txt -c backend/constraints.txt
#
# Bộ test tầng A (mặc định) KHÔNG nạp model thật, chạy ~10-15 s:
#     python -m pytest
# ============================================================================

-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
